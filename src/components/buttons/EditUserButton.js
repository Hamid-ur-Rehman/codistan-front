'use client'
const EditUserButton = ({ id }) => {
    return (
        <button
            className=" btn-primary rounded-md outline  bg-gray-600 text-white py-1 px-5 self-end">Edit User</button>
    );
};

export default EditUserButton;
